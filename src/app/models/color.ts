export class Color {
    Name: string;
    Identity: string;
    Checked: boolean;

    constructor(name: string, identity: string) {
        this.Name = name;
        this.Identity = identity;
        this.Checked = false;
    }

    
}