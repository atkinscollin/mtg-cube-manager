import { JsonCard } from "./json-card";

export class JsonExpansion {
    cards: JsonCard[];
    code: string;
    gathererCode: string;
    name: string;
    onlineOnly: boolean;
    type: string;
    
    constructor() { }
}